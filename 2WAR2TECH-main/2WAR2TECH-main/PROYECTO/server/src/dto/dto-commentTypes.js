import { Type } from '@sinclair/typebox';
/*
export const ASSIGNMENTXSTUDENTIdDTOSchema = Type.Integer({
    errorMessage: {
        type: 'El tipo de "ASSIGNMENTXSTUDENTId" no es válido, debe ser un integer',
    },
});

export const USERIdDTOSchema = Type.Integer({
    errorMessage: {
        type: 'El tipo de "USERId" no es válido, debe ser un integer',
    },
});

export const commentDTOSchema = Type.String({
    minLenght: 1,
    maxLenght: 255,
    errorMessage: {
        type: 'El tipo de "comment" no es válido, debe ser un string',
        minLenght: 'El atributo "comment" no puede ser nulo',
        maxLenght: 'La longitud de "comment" debe ser menor a 255 caracteres'
    },
});
*/